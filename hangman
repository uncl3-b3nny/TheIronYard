#Hangman
puts "Please enter either easy, medium, or hard"

turns_left = nil
difficulty = gets.chomp

if difficulty = "easy"
	turns_left = 15
else if difficulty == "medium"
	turns_left = 10
else turns_left = 5
end

puts "Enter one word!"
word=gets.chomp # this needs to be hidden from view
puts blank_array = "_ "*word.length
puts blank_array = blank_array.split(//)

word_arrary = word.split(//)


puts "Guess a letter!"
guess1=gets.chomp

if guess1 blank_array[0]=guess1
	blank_array[0].replace(guess1)
else puts turns_left - 1
end
